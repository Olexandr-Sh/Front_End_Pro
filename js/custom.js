var height = prompt('enter the height of the cylinder');
var radius = prompt('enter the radius of the cylinder');
alert('Cylinder volume with height ' + height + 'sm and radius ' + radius + 'sm is equal ' + Math.PI * (radius * radius) * height);